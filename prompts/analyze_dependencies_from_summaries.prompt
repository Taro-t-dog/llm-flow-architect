以下の「各タスクの要約」を読み、タスク間の依存関係を分析せよ。

# 指示
- 結果は、キーがタスク名、バリューがそのタスクが依存するタスク名の配列となるJSON形式で出力すること。
- 依存関係がないタスクのバリューは空の配列 `[]` とすること。
- 最重要: 他のいかなるテキストも出力せず、純粋なJSONオブジェクトのみを出力せよ。

# 各タスクの要約
---
{summaries_json_str}
---

# 依存関係JSON